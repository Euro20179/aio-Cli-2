#!/bin/bash

: "${AIO_API="\"https://aioapi.seceurity.place\""}"
: "${AIO_APILEN=$((${#AIO_API}-2))}"

gcc -o aio-cli \
    -Wall \
    -std=c99\
    -D"AIO_API=${AIO_API}" -D"AIO_APILEN=${AIO_APILEN}"\
    -O2\
    `pkg-config --libs json-c --cflags json-c vips` -lcurl -lsixel -lselector\
    "$@" ./**/*.c ./*.c
